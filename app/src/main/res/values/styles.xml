<resources>

    <style name="Widget.Theme.GreenMerge.ActionBar.Fullscreen" parent="Widget.AppCompat.ActionBar">
        <item name="android:background">@color/black_overlay</item>
    </style>

    <style name="Widget.Theme.GreenMerge.ButtonBar.Fullscreen" parent="">
        <item name="android:background">@color/black_overlay</item>
        <item name="android:buttonBarStyle">?android:attr/buttonBarStyle</item>
    </style>
    <style name="wms_base_button">
        <item name="android:layout_width">wrap_content</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:minWidth">@dimen/dp_80</item>
        <item name="android:paddingStart">@dimen/dp_8</item>
        <item name="android:paddingEnd">@dimen/dp_8</item>
        <item name="android:paddingTop">@dimen/dp_4</item>
        <item name="android:paddingBottom">@dimen/dp_4</item>
        <item name="android:textSize">@dimen/dp_16</item>
        <item name="android:gravity">center</item>
    </style>

    <!-- 普通空心按钮（如：TextView/Button），白底+蓝边+蓝色字体-->
    <style name="wms_normal_button_stroke_4a556b" parent="wms_base_button">
        <item name="text_color_normal">@color/color_4a556b</item>
        <item name="text_color_pressed">@color/color_4A556B_50</item>
        <item name="background_normal">@color/white</item>
        <item name="border_color_normal">@color/color_4a556b</item>
        <item name="border_color_pressed">@color/color_4A556B_50</item>
        <item name="border_width_normal">1dp</item>
        <item name="corner_radius">@dimen/dp_4</item>
    </style>

    <!-- RTextView 实心按钮，蓝底+白色字体-->
    <style name="wms_r_text_button_solid_4a556b" parent="wms_base_button">
        <item name="android:textColor">@color/white</item>
        <item name="background_normal">@color/color_4a556b</item>
        <item name="corner_radius">@dimen/dp_4</item>
        <item name="background_pressed">@color/color_4A556B_50</item>
    </style>

    <!--ActionSheet Dialog样式-->
    <style name="ActionSheetDialogStyle" parent="@android:style/Theme.Dialog">
        <!-- 背景透明 -->
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowContentOverlay">@null</item>
        <!-- 浮于Activity之上 -->
        <item name="android:windowIsFloating">true</item>
        <!-- 边框 -->
        <item name="android:windowFrame">@null</item>
        <!-- Dialog以外的区域模糊效果 -->
        <item name="android:backgroundDimEnabled">true</item>
        <!-- 无标题 -->
        <item name="android:windowNoTitle">true</item>
        <!-- 半透明 -->
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:backgroundDimAmount">0.4</item>
    </style>

    <style name="dialogBase" parent="@android:style/Theme.Dialog">
        <!-- 边框 -->
        <item name="android:windowFrame">@null</item>
        <!-- 是否浮现在activity之上 -->
        <item name="android:windowIsFloating">true</item>
        <!-- 无标题 -->
        <item name="android:windowNoTitle">true</item>
        <!-- 背景透明 -->
        <item name="android:windowBackground">@android:color/transparent</item>
        <!-- 半透明 -->
        <item name="android:windowIsTranslucent">false</item>
        <!-- 变暗 -->
        <item name="android:backgroundDimEnabled">true</item>

        <item name="android:backgroundDimAmount">0.4</item>
    </style>
</resources>